# Mission Framework operational loader

For substantive work affecting `froekjaer/mission-framework` or a repository explicitly identified as part of the same mission:

1. Retrieve and follow the current `main:docs/operational/OP-001-Mission-Operational-Preamble.md` from `froekjaer/mission-framework`.
2. Do not use remembered repositories, paths, identifiers, decisions or structures operationally without verification.
3. Search authoritative state before creating or modifying artefacts.
4. Stop the affected action when required state cannot be verified.
5. After a write, read back and verify the resulting state where tooling permits.
6. Show only a compact preamble status; do not reproduce OP-001 unless requested.

Do not activate this loader for routine conversation that cannot change operational or canonical state.
